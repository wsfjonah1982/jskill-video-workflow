# Secrets
credential*.json
# ...except the placeholder template, which is meant to be committed as a setup reference
!credential_tmp.json

# Runtime folders (uploads, generated outputs, logs) — anything prefixed with "_"
_*/

# Python
__pycache__/
*.pyc
